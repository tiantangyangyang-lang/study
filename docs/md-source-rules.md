# Markdown 来源规则

## 允许读取的目录

- `D:\work\Kaoyan-Math2-Papers`
- `D:\work\Kaoyan-Math1-Papers`

## 允许读取的文件类型

仅 `.md` 和 `.markdown`。

## 明确忽略的文件类型

- `.pdf`
- `.png`
- `.jpg`
- `.jpeg`
- `.webp`
- `.docx`
- 其他所有非 `.md` / `.markdown` 文件

## subject 映射

| 来源目录 | subject |
|---|---|
| `D:\work\Kaoyan-Math2-Papers` | `math2` |
| `D:\work\Kaoyan-Math1-Papers` | `math1` |

## 只读承诺

本项目绝不修改来源目录中的任何文件，不创建、删除、重命名来源目录中的文件。

## 不 OCR

不读取 PDF、图片，也不调用 OCR。

## 不读取 D:\work\kaoyan

原项目 `D:\work\kaoyan` 不在本次来源范围内，不读取其中任何文件。
